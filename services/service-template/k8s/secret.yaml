apiVersion: v1
kind: Secret
metadata:
  name: service-template-secret
  namespace: islamic-financing
  labels:
    app: service-template
type: Opaque
stringData:
  # Database credentials (base64 encoded in production)
  db.username: "service_template_user"
  db.password: "ChangeMeInProduction123!"

  # API keys (if needed)
  api.key: "your-api-key-here"

  # JWT signing key (if service generates tokens)
  jwt.secret: "your-jwt-secret-here"